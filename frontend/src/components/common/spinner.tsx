import React from "react";

const Spinner = () => {
  return (
    <span className="ring-2 ring-accent-foreground border-t-2 size-5 animate-spin rounded-full"></span>
  );
};

export default Spinner;
